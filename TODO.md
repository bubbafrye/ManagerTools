# To-do

- [ ] **Theme save system** — Wire the save (“add theme”) control to persist custom looks (storage, naming, and preset slots). Drag-to-delete for preset swatches is in place; saved themes should use the same remove path.
- [ ] **Humanize random** — Consider a “humanize” randomizer that varies the *active* palette (subtle shifts within the current theme) instead of a fully unconstrained rando.
